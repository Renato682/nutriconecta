spring:
  application:
    name: nutriconecta
  datasource:
    url: jdbc:mysql://localhost:3306/dbnutriconecta?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: "Otaner67$"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect